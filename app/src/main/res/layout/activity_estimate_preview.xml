<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="0dp"
        android:layout_height="?attr/actionBarSize"
        android:background="#0066cc"
        app:title="Estimate Preview"
        app:titleTextColor="@android:color/white"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <ScrollView
        android:id="@+id/scrollView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/toolbar"
        app:layout_constraintBottom_toTopOf="@id/btnDownloadPdf"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:id="@+id/previewContainer"
            android:orientation="vertical"
            android:padding="16dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <!-- Business Info -->
            <TextView android:text="Business Information" android:textStyle="bold" android:textSize="16sp" />
            <TextView android:id="@+id/tvBusinessInfo" android:layout_marginBottom="8dp"/>

            <!-- Customer Info -->
            <TextView android:text="Customer Information" android:textStyle="bold" android:textSize="16sp" />
            <TextView android:id="@+id/tvCustomerInfo" android:layout_marginBottom="8dp"/>

            <!-- Estimate Info -->
            <TextView android:text="Estimate Details" android:textStyle="bold" android:textSize="16sp" />
            <TextView android:id="@+id/tvEstimateInfo" android:layout_marginBottom="8dp"/>

            <!-- Line Items -->
            <TextView android:text="Estimate Lines" android:textStyle="bold" android:textSize="16sp" />
            <LinearLayout
                android:id="@+id/linesContainer"
                android:orientation="vertical"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"/>

            <!-- Totals -->
            <TextView android:text="Totals" android:textStyle="bold" android:textSize="16sp" />
            <TextView android:id="@+id/tvTotals"/>

        </LinearLayout>
    </ScrollView>

    <Button
        android:id="@+id/btnDownloadPdf"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Download PDF"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:backgroundTint="#0066cc"
        android:textColor="@android:color/white"/>

</androidx.constraintlayout.widget.ConstraintLayout>